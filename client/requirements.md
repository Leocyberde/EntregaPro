## Packages
framer-motion | For smooth animations and transitions
lucide-react | Icons for the UI
react-hook-form | Form handling
zod | Schema validation
@hookform/resolvers | Zod resolver for react-hook-form
clsx | Class utility
tailwind-merge | Class utility

## Notes
The app requires a dark/orange theme as requested.
Pix payment simulation is handled via a button that hits the webhook endpoint directly for demo purposes.
Auth requires keeping track of the user state globally.
